{
  "name": "jest-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest --no-cache"
  },
  "dependencies": {
    "jest-cli": "^12.1.1",
    "randomstring": "^1.1.5"
  }
}
